extends layout 

block content 

    h1 Genre:  #{genre.name}

    div
        h4 Books 

        ul.book-list
            each book in genre_books 
                li.item 
                    a(href=book.url) #{book.title}
                    p.summary #{book.summary}
            
            else 
                p This genre has no books.