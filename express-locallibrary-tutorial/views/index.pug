extends layout

block content
  h1= title
  p Welcome to #[em LocalLibrary], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    ul.stats
      li.stat-item #[strong Books:] !{data.book_count}
      li.stat-item #[strong Copies:] !{data.book_instance_count}
      li.stat-item #[strong Copies available:] !{data.book_instance_available_count}
      li.stat-item #[strong Authors:] !{data.author_count}
      li.stat-item #[strong Genres:] !{data.genre_count}
